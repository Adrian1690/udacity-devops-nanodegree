pipeline {
    agent any
    stages {
        
        stage('Create a deployment and service to expose image created') {
            steps{
                sh 'sudo docker image ls'
            }
        }
    }
}